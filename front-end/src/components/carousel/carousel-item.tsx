/** @format */

import { ReactNode } from "react";
import { WhapperCarouselItem } from "./styles";

export default ({ children }: { children: ReactNode }) => {
  return <WhapperCarouselItem>{children}</WhapperCarouselItem>;
};
