import { DialogTitle, Grid, styled } from "@mui/material";

export const ModalTitle = styled(DialogTitle)({});

export const WhapperModalOffice = styled(Grid)({
  width: "100%",
  padding: "10px",
});
